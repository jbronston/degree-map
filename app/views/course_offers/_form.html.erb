<%= form_for(@course_offer) do |f| %>
  <% if @course_offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_offer.errors.count, "error") %> prohibited this course_offer from being saved:</h2>

      <ul>
      <% @course_offer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %><br />
    <%= f.number_field :course_id %>
  </div>
  <div class="field">
    <%= f.label :instructor %><br />
    <%= f.text_field :instructor %>
  </div>
  <div class="field">
    <%= f.label :year %><br />
    <%= f.number_field :year %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
